package my-namespcae:my-package;

interface my-interface {
  variant my-variant {
    rename-new-name(string),
    retype(f32),
    retype-remove-type,
    added(option<string>),
    from-list-a(list<bool>),
    from-list-b(string),
  }
}
